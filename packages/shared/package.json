{
  "name": "@sla-monitoring/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and constants for the SLA Monitoring Dashboard workspaces",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^3"
  }
}

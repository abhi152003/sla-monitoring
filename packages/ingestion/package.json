{
  "name": "@sla-monitoring/ingestion",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic CSV processing library implementing the SLA Monitoring data-cleaning and SLA rules (R1-R27)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@sla-monitoring/shared": "*"
  },
  "devDependencies": {
    "@eslint/js": "^9",
    "eslint": "^9",
    "typescript": "^5",
    "typescript-eslint": "^8",
    "vitest": "^3"
  }
}

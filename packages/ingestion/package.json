{
  "name": "@sla-monitoring/ingestion",
  "version": "0.1.0",
  "private": true,
  "description": "CSV ingestion pipeline for the SLA Monitoring Dashboard (placeholder - implemented in later work orders)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^3"
  }
}

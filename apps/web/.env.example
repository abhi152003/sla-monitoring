# Web environment (local development template)
# Copy to .env.local and fill in the API base URL. Never commit credentials.

# Local Worker API. For a deployed web app, use the deployed Worker URL below.
NEXT_PUBLIC_WORKER_URL=http://localhost:8787

# Production example:
# NEXT_PUBLIC_WORKER_URL=https://sla-monitoring-worker.abhiprajapati011.workers.dev

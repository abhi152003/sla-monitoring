# Worker environment (local development template)
# Copy to .dev.vars and fill in real values. Never commit real credentials.

# Neon PostgreSQL connection string (used by later work orders)
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require
